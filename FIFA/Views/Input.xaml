<Page
    x:Class="FIFA.Views.Input"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FIFA.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Left" Orientation="Horizontal" Margin="0 10 0 10">
            <TextBlock Text="Semana:" Margin="20 0 0 0"/>
            <TextBlock x:Name="weekNumber" Text="#####" Margin="5 0 0 0"/>
            <TextBlock Text="Cantidad total: " Margin="20 0 0 0"/>
            <TextBlock x:Name="weekTotal" Text="#####" Margin="5 0 0 0"/>
            <Button x:Name="AddNewRow" Content="Insertar otro lote" Margin="5 0 0 0" Width="Auto" Click="AddNewRow_Click" />
        </StackPanel>

        <controls:DataGrid Grid.Row="1" Margin="15" ItemsSource="{x:Bind SomeList}" IsReadOnly="False" AutoGenerateColumns="False">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Lote" Binding="{Binding Lote}"/>
                <controls:DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}"/>
                <controls:DataGridTemplateColumn Header="Name">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CalendarDatePicker Date="{Binding Fecha, Mode=TwoWay}" />
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                    <controls:DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <CalendarDatePicker Date="{Binding Fecha, Mode=TwoWay}" />
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellEditingTemplate>
                </controls:DataGridTemplateColumn>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</Page>
